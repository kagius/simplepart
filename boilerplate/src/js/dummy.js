/**
 * @ngdoc service
 * @name dummy
 * @description A dummy object to contain some dummy functions
 */
var dummy = {
	/**
	 * @ngdoc method
	 * @name dummy:sum
	 * @methodOf dummy
	 * @description Returns the total of two numbers
	 * @param {number} a - The first number
	 * @param {number} b - The second number
	 */
	sum: function(a, b) {
		return a + b;
	}
};